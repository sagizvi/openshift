kind: Service
apiVersion: v1
metadata:
# set your user name
  name: user2-hello-world
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  selector:
# set your user name
    app: user2-hello-world